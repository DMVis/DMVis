// Defined enums for increased type safety

export enum OriginX {
  Left,
  Middle,
  Right
}

export enum OriginY {
  Top,
  Middle,
  Bottom
}

export enum ColumnType {
  Bar = 'Bar',
  Rank = 'Rank',
  Select = 'Select',
  Separator = 'Separator',
  Sum = 'Sum',
  Text = 'Text'
}

export enum IconType {
  Filter = 'filter',
  Group = 'group',
  More = 'more',
  Search = 'search',
  Sort = 'sort',
  Weight = 'weight'
}
