export enum OriginX {
  Left,
  Middle,
  Right
}

export enum OriginY {
  Top,
  Middle,
  Bottom
}

export enum ColumnType {
  Rank = 'Rank',
  Select = 'Select',
  Text = 'Text',
  Bar = 'Bar',
  Sum = 'Sum',
  Separator = 'Separator'
}
